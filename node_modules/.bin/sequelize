#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../sequelize/bin/sequelize" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../sequelize/bin/sequelize" "$@"
  ret=$?
fi
exit $ret
